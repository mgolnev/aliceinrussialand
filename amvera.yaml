meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile

run:
  containerPort: 3000
  # В эту директорию пишутся загруженные изображения; она сохраняется между пересборками.
  persistenceMount: /app/public/media
